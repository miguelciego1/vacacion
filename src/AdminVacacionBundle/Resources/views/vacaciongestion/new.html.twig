{% extends 'base.html.twig' %}

{% block page_content %}
<div class="row">
              <div class="col-md-5 col-md-offset-3 col-sm-5 col-sm-offset-3 col-xs-5 col-xs-offset-3">
                <div class="x_panel">
                  <div class="x_title">
                    <h2>Gestion <small>Registro de Gestion</small></h2>
                    <div class="clearfix"></div>
                  </div>
                  <div class="x_content">
                    {{ form_start(form) }}
                      <div class="ln_solid"></div>
                      <div class="form-group">
                        <div class="col-md-8 col-md-offset-2">
                            {{ form_widget(form) }}

                            <a href="{{path('vacaciongestion_index')}}" class="btn btn-default"><i class="fa fa-repeat"></i> Volver</a>

                            <input type="submit" class="btn btn-success" value="Guardar">


                        </div>
                      </div>
                    {{ form_end(form) }}
                  </div>
                </div>
              </div>
            </div>

{% endblock %}
{% block javascripts %}
			    {{ form_javascript(form) }}
{% endblock javascripts %}
